kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: xml-mgmt
spec:
  to:
    kind: Service
    name: mgmt
  port:
    targetPort: xml-mgmt
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect
