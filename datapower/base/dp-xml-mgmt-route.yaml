kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: dp-xml-mgmt
spec:
  to:
    kind: Service
    name: dp-mgmt
  port:
    targetPort: xml-mgmt
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect
